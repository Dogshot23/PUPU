# User-created content

Reserved for future user-created modules. Same shape as `/packs`:
a folder per module, each containing `module.json` and `thoughts.json`,
registered in `../registry.json` once ready.

Not used yet — no modules currently live here.
